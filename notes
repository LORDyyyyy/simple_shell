
Check memory leak command:

valgrind --leak-check=full --track-origins=yes ./a.out

valgrind -s --leak-check=full --show-leak-kinds=all ./a.out

valgrind -s ./a.out

Compilation command:

gcc -Wall -Werror -Wextra -pedantic -std=gnu89 *.c -o hsh

gcc -Wall -Werror -Wextra -pedantic -std=gnu89 *.c

gcc *.c


1. in main.c,  DONE
	in the very end in shell_loop func, 
	free data->cmd, 
	which is a string array

2. fix memory leak. DONE

3. fix whitespaces as input with the get_location function.

